# Next.js client -> API
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# E2E only: from `stripe listen --forward-to http://localhost:3001/webhooks/stripe`
STRIPE_WEBHOOK_SECRET=whsec_xxx
